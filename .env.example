# Copy to .env and fill in. Never commit .env.
#
# This project has no required environment variables — no API keys, no
# database, no external services. Everything (target URL, concurrency,
# rate limit, retries, robots.txt behavior) is configured via CLI flags;
# see `async-crawler --help`.
